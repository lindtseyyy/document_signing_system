{
  "name": "document-signing-system-backend",
  "version": "1.0.0",
  "description": "Backend API for DSA key generation, signing, and verification (in-memory only).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "multer": "^1.4.5-lts.1"
  }
}
